{
  "name": "eisenhower-matrix-widget",
  "version": "1.0.0",
  "description": "FigJam Eisenhower Matrix widget with collaborative voting",
  "scripts": {
    "build": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js --target=es6",
    "watch": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js --target=es6 --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.84.0",
    "@figma/widget-typings": "^1.9.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
